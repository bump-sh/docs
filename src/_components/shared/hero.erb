<page-hero class="full-bleed wrapper">
  <h1><%= @title %></h1>
  <% if @description.present? %>
    <p><%= @description %></p>
  <% end %>
  <%= render Shared::Taglist.new(tags: @tags) %>
</page-hero>
